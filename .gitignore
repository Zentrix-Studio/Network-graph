# Dependencies & build output
node_modules
dist
.tmp

# OS / editor junk
.DS_Store

# pbiviz build-analyzer reports (generated by pbiviz package)
webpack.statistics.*.html

# ── Local-only: dev/build tooling, tests, docs & QA scratch. Kept on disk to
# ── work with, but deliberately OFF the published GitHub repo, which holds ONLY
# ── the shippable visual (src/, style/, assets/ + build config). See
# ── visuals/CLAUDE.md.
test/
harness/
scripts/
docs/
qa-evidence/
qa-reports/
.claude/
CLAUDE.md
jest.config.js
tsconfig.spec.json
